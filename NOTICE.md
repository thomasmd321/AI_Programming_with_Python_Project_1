# What the license covers

The [MIT License](LICENSE) covers Thomas Stewart's own work in this
repository: the code he wrote, his changes to the Udacity-supplied files, the
tests, the notebook, the CI configuration and the documentation.

It does **not** cover material from Udacity's *AI Programming with Python*
Nanodegree starter project, which stays under Udacity's terms:

- `workspace/print_functions_for_lab_checks.py` and `workspace/test_classifier.py`
- the original versions of `workspace/classifier.py`, `workspace/run_models_batch.sh`
  and `workspace/run_models_batch_uploaded.sh`
- the starter templates (function signatures and docstrings) the pipeline
  modules were built from
- `workspace/pet_images/`, `workspace/dognames.txt` and
  `workspace/imagenet1000_clsid_to_human.txt`

The photos in `workspace/uploaded_images/` are not covered either; their
copyright belongs to their original owners.

The pretrained models are downloaded from torchvision at run time and are not
part of this repository; see the [torchvision model
documentation](https://pytorch.org/vision/stable/models.html) for their terms.
